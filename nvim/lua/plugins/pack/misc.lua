-- Plugins que solo necesitan `setup()` y no dan para archivo propio.

require('guess-indent').setup {} -- detecta la indentación del archivo
require('fidget').setup() -- progreso del LSP en la esquina
